runtime: nodejs
env: flex
service: cors-anywhere

manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 2
  disk_size_gb: 10

env_variables:
  CORSANYWHERE_WHITELIST: 'https://app.indie.tools,http://localhost:3000'
